<div class="row gradiente1">
 <br>
 <center><h4>{{ mensaje }}</h4></center>
 <br>
</div>

<br>

<!-- Slider de Productos Destacados -->
<div class="slider">

   <!-- Obtengo las Imagenes -->
   {% if producto.getImagens()|length > 0 %}      
      <!-- Iteramos sobre todas las imagenes conseguidas -->
      <ul class="slides">
      {% for imagen in producto.getImagens() %}              
       <li><img class="responsive-img" src="{{ asset(imagen.url) }}"></li>
      {% endfor %}
      </ul>
      <!-- Obtengo las Imagenes -->             
   {% endif %}
    
</div>
<!-- Slider de Productos Destacados -->

<br>
